class Solution {
    public boolean stoneGame(int[] piles) {
        
        return true; // if it is even then always the first will win










        // int evenSum = 0;
        // int oddSum = 0;
        
        // // Calculate the sum of stones in even-indexed and odd-indexed piles
        // for (int i = 0; i < piles.length; i++) {
        //     if (i % 2 == 0) {
        //         evenSum += piles[i];
        //     } else {
        //         oddSum += piles[i];
        //     }
        // }
        
        // // Alice can always choose the set (even-indexed or odd-indexed) with the higher sum
        // return true; // Alice always wins by choosing the optimal set

    }
}